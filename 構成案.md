# まごころ司法書士事務所 デモサイト 構成案 v1.1

> **v1.1での変更点**：UI/UXデザイナー役・フロントエンドエンジニア役のサブエージェントレビューを反映。パンくずリスト追加（§2.6）、フォントサイズ/タップ領域の数値定義（§8）、業務ページ別トーン差別化（§7-5）、問い合わせフォーム項目の絞り込み（§7-12）、ヘッダーCTA位置ズレ対策（§2.3）、ロゴモチーフ確定（§1.4）、ハンバーガーメニューのキーボード操作・フォーカストラップ仕様（§2.4）、GSAP CDN障害時フェイルセーフ（§4.6）、フォームアクセシビリティ（§7-12）、フォント切替CLS対策（§1.3）、ScrollTrigger初期化タイミング（§4.7）、共通部分の一貫性運用ルール（§5）、SEO追記（§9）を追加。

> 本ドキュメントは「LP型×複数ページ型ハイブリッド」の司法書士事務所デモサイトを実装するための詳細仕様書。
> このプロンプト（本ファイル）をそのままエンジニアに渡せば実装が完結するレベルの粒度で記述する。
> **本サイトは完全な架空のデモンストレーション用サイトである。** 掲載する事務所名・人物名・住所・電話番号・実績数値・お客様の声はすべて架空のものとし、全ページのフッターに「本サイトはデモンストレーション用に作成された架空の司法書士事務所紹介サイトです。掲載内容はすべてフィクションです。」を明記する。

---

## 0. サイト概要

| 項目 | 内容 |
|---|---|
| サイト名 | まごころ司法書士事務所 |
| サイトタイプ | LP型（home 1ページで完結）× 複数ページ型（ハンバーガーメニューから独立した詳細ページ群へ遷移）のハイブリッド |
| 目的 | 司法書士事務所の営業用デモサイトのテンプレート・提案サンプル |
| 技術 | 静的サイト（HTML5 / CSS3 / Vanilla JS）。ビルドツール・フレームワーク不使用。CDN経由で Google Fonts と GSAP(+ScrollTrigger) のみ読み込む |
| 画像 | 今回は一切使用しない。すべて「画像プレースホルダー枠」として実装し、後から本人がAI生成画像を差し込める構造にする（詳細は §5） |
| ページ数 | 全13ページ（home 1 + 詳細ページ群 12） |
| 対応デバイス | モバイルファースト。375px〜1440px超まで対応するレスポンシブ |

---

## 1. ブランド定義

### 1.1 トーン&マナー
「誠実・信頼重視の伝統派」。年配層〜相続世代にも安心感を与える、格式と温かみを両立した士業デザイン。派手な演出は避け、余白と質の高いタイポグラフィで信頼感を作る。

### 1.2 配色（深紺 × ゴールドアクセント）

CSS変数として定義し、全ページ共通の `variables.css`（または各HTMLの`<style>`冒頭）に記述する。

```css
:root {
  /* ベース */
  --color-navy-900: #14213D;   /* メインカラー：最も深い紺（ヘッダー、見出し、フッター背景） */
  --color-navy-700: #1F3159;   /* 紺：ボタン、強調テキスト */
  --color-navy-500: #35507F;   /* 紺（明）：サブテキスト、線 */
  --color-navy-100: #E9ECF3;   /* 薄紺：セクション背景の区切り */

  /* アクセント */
  --color-gold-600: #A9812F;   /* ゴールド濃：ボタンホバー、線装飾 */
  --color-gold-500: #B08D57;   /* ゴールド：アクセント、アイコン、装飾線 */
  --color-gold-100: #F3EAD8;   /* 薄ゴールド：バッジ背景、カード背景アクセント */

  /* ベース背景・テキスト */
  --color-bg: #FBFAF7;         /* オフホワイト：ページ全体の背景 */
  --color-surface: #FFFFFF;    /* カード・パネル背景 */
  --color-text-main: #2A2A2A;  /* 本文テキスト */
  --color-text-sub: #6B6B6B;   /* 補足テキスト */
  --color-border: #E3DFD6;     /* 罫線 */

  /* 状態色 */
  --color-success: #3D7A5C;
  --color-danger: #B0453E;
}
```

### 1.3 フォント

Google Fonts より CDN 読み込み。

```html
<link rel="preconnect" href="https://fonts.googleapis.com">
<link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
<link href="https://fonts.googleapis.com/css2?family=Noto+Serif+JP:wght@500;700&family=Noto+Sans+JP:wght@400;500;700&display=swap" rel="stylesheet">
```

- 見出し（h1〜h3、ロゴ、キャッチコピー）：`"Noto Serif JP", serif`
- 本文・UI・ボタン・ナビ：`"Noto Sans JP", sans-serif`
- 数値カウントアップ部分（実績数字）：`"Noto Serif JP", serif`（和数字の重厚感を出す）
- `font-display: swap`によるFOUT時のレイアウトシフト（CLS）を軽減するため、フォールバックフォント（`serif`/`sans-serif`）とNoto系のグリフ寸法差を`font-size-adjust`または近似の`line-height`調整で最小化する。読み込み完了まではシステムフォントで違和感なく表示されることを優先する。

### 1.4 ロゴ／サイトマーク

画像不使用、CSS+SVGのみで完結。

- テキスト：「まごころ」を `Noto Serif JP` bold・紺色で表示し、その右横に小さく「司法書士事務所」を `Noto Sans JP` で添える2段組み。
- アイコン（確定案）：ロゴ左に、SVGで描いた直径28px程度の円形モチーフ＝「朱印風の二重丸」（外円+内円の同心円、内円の中に短い縦線を1本添えて印影を想起させる）を配置。線幅1.5px、色は `--color-gold-500`。他案（手を重ねるモチーフ、和柄線画）は不採用とし、実装時はこの二重丸案のみをSVGコーディングする。
- ヘッダー内では高さ40〜48px、フッターでは56〜64pxで使用。
- favicon代わりにも同アイコンをSVGで簡易生成する。

### 1.5 装飾ビジュアル言語（画像の代わり）

- ヒーローや各セクション背景に、和紙のようなうっすらしたグラデーション（`--color-navy-900`→`--color-navy-700`の斜めグラデーション、または`--color-bg`寄りの薄いグラデーション）。
- SVGの細線サークル・青海波（波紋）風のパターンをセクション区切りにあしらう（opacity 0.06〜0.12程度で控えめに）。
- 装飾はあくまで背景。主役は画像プレースホルダー枠（§5）であり、装飾と混同しないよう視覚的に分離する。

---

## 2. サイト構造・情報設計

### 2.1 サイトマップ（全13ページ）

```
/ (index.html)                        … home（LP）
├── about.html                        … 事務所紹介
├── services.html                     … 業務案内トップ（3カテゴリの入口）
│   ├── services-souzoku.html         … 相続・遺言サポート 詳細
│   ├── services-fudousan.html        … 不動産登記 詳細
│   └── services-houjin.html          … 法人登記 詳細
├── pricing.html                      … 料金案内
├── flow.html                         … ご相談の流れ
├── voice.html                        … お客様の声・解決事例
├── faq.html                          … よくあるご質問
├── column.html                       … コラム一覧（記事4本、サンプル）
├── access.html                       … アクセス
└── contact.html                      … お問い合わせ（無料相談予約フォーム）
```

### 2.2 グローバルナビゲーション（ハンバーガーメニュー）

**重要な仕様**：ハンバーガーメニューの各項目は、homeの該当セクションへのアンカーリンクではなく、**必ず独立した詳細ページへ遷移する**。home自体のヘッダーにはロゴと「無料相談予約」ボタン、ハンバーガーアイコンのみを常設し、home内のセクションアンカーへの誘導は本文中の「詳しくはこちら▶」ボタンのみで行う（ナビ項目とは分離する）。

メニュー項目（クリック→遷移先）：

1. 事務所紹介 → `about.html`
2. 業務案内 → `services.html`（サブ項目としてホバー/タップで相続・不動産・法人の3リンクを展開してもよいが、モバイルではアコーディオンで3リンクを表示）
   - 相続・遺言サポート → `services-souzoku.html`
   - 不動産登記 → `services-fudousan.html`
   - 法人登記 → `services-houjin.html`
3. 料金案内 → `pricing.html`
4. ご相談の流れ → `flow.html`
5. お客様の声 → `voice.html`
6. よくあるご質問 → `faq.html`
7. コラム → `column.html`
8. アクセス → `access.html`
9. お問い合わせ → `contact.html`
10. （ナビ最下部に常設ボタン）無料相談予約 → `contact.html`（ボタンスタイルで視覚的に分離）

### 2.3 ヘッダー仕様（全ページ共通・スクロール連動アニメーション付き）

- 初期状態：ページ最上部では背景透明、テキスト・ロゴは白抜き（home のヒーローが紺背景のため）。詳細ページ（ヒーローが白背景）では初期状態から白背景+紺文字で表示し分岐する（`data-header-theme="dark-hero"` 属性で制御）。
- スクロール50px以上で `.is-scrolled` クラスを付与 → 背景が白（`--color-surface`、shadowあり）に変化、ロゴ・ヘッダー高さが 88px→64px に縮小、テキスト色が紺に変化。GSAPで0.3〜0.4s、ease `power2.out` で滑らかに遷移。
- 右側に「無料相談予約」ボタン（ゴールド背景、紺文字）を常設。その左に電話番号リンク（アイコン+電話番号、`tel:`リンク、タブレット以上で表示）。
- 右端にハンバーガーアイコン（3本線→開くと×に変形、GSAPで回転+フェード）。
- **誤タップ対策**：高さ変化（88px→64px）に伴いCTAボタン・電話番号リンクの縦位置がわずかに動くため、遷移アニメーション中（0.3〜0.4s間）はヘッダー右側の操作要素に `pointer-events: none` を一時付与し、遷移完了後に復帰させる。またボタン自体は上下中央寄せ（`align-items:center`）を常に維持し、フォントサイズは高さ変化させずアイコン/パディングのみで高さを吸収することで、ボタンテキストの縦揺れを最小化する。

### 2.4 ハンバーガーメニュー展開UI

- モバイル：画面右からフルスクリーンでスライドイン（紺背景、白文字、GSAP `xPercent: 100 → 0`）。メニュー項目は`stagger`で0.05s刻みにフェード&ライズしながら出現。
- デスクトップ（1024px以上）：右側から幅420px程度のオフキャンバスパネルがスライドイン。背景はオーバーレイ（`rgba(20,33,61,0.6)`）でクリックすると閉じる。
- メニュー内最下部に電話番号・営業時間・SNS等（今回はダミーでOK）を表示。
- 各ページのグローバルナビ内、現在表示中のページに対応する項目には `aria-current="page"` を付与し、視覚的にもゴールドの下線/背景で強調する（現在地の可視化）。

### 2.4.1 キーボード操作・フォーカス管理（実装必須）

- ハンバーガーボタンには `aria-expanded`（開閉状態）と `aria-controls="global-nav"` を付与し、開閉に応じて`aria-expanded`の値を`true`/`false`で切り替える。
- メニューを開いた瞬間、フォーカスをメニュー内の最初のリンク（またはメニュー自体の閉じるボタン）へ移動する。
- メニュー表示中は `Tab`/`Shift+Tab` がメニュー内の要素だけを循環するフォーカストラップを実装する（メニュー外の背景コンテンツへフォーカスが漏れないようにする）。
- `Escape` キー押下でメニューを閉じ、フォーカスをハンバーガーボタンへ戻す。
- オーバーレイ（背景）クリックでも閉じられるようにし、閉じた際も同様にフォーカスをハンバーガーボタンへ戻す。
- メニューパネルには `role="dialog"` `aria-modal="true"` `aria-label="サイトメニュー"` を付与する。

### 2.5 フッター仕様（全ページ共通）

- 背景 `--color-navy-900`、白文字。
- 4カラム構成（モバイルは縦積みアコーディオンなし・そのまま縦並び）：
  1. ロゴ＋事務所紹介一言＋住所・電話（ダミー）
  2. サイトマップリンク（業務案内/料金/流れ/お客様の声/FAQ）
  3. サイトマップリンク（事務所紹介/コラム/アクセス/お問い合わせ）
  4. 「無料相談予約」CTAボックス（ボタン+電話番号)
- 最下部中央に `© 2026 まごころ司法書士事務所（架空の事務所です）` とデモ免責文をセンター配置、小さめの文字で常設。

### 2.6 パンくずリスト（detailページ必須・home除く）

高齢層ユーザーが多いことを踏まえ、ハンバーガー経由で直接detailページへ着地しても現在地が分かるよう、**home以外の全12ページのヒーロー帯直下**に簡易パンくずを設置する。

- 表示形式：`ホーム ＞ 業務案内 ＞ 相続・遺言サポート`（各項目はリンク、最後の現在ページのみリンクなし・太字）
- 2階層になるページ（`services-souzoku.html`等）は `ホーム ＞ 業務案内 ＞ ○○` の3項目、それ以外（`about.html`等）は `ホーム ＞ ○○` の2項目。
- マークアップは`<nav aria-label="パンくずリスト"><ol>...</ol></nav>`。CSSでは`li`間に`--color-gold-500`の`▶`区切りを`::after`で表示。
- モバイルでは横スクロール可能な1行、または長い場合は現在ページの1階層上のみ表示する省略形（`＜ 業務案内へ戻る`）でも可とする。

---

## 3. 画像プレースホルダー仕様（重要）

画像を後から迷わず差し込めるよう、**すべての画像枠に「推奨する画像内容」を明記したラベル付きプレースホルダー**を実装する。単なる空のグレーボックスにはしない。

### 3.1 共通マークアップパターン

```html
<div class="img-slot" style="--ar: 16/9;" data-img-hint="事務所の外観写真（日中・入口付近から建物全体が入るアングル）">
  <div class="img-slot__frame">
    <svg class="img-slot__icon" width="32" height="32" viewBox="0 0 24 24"><!-- 画像アイコン --></svg>
    <p class="img-slot__label">事務所の外観写真</p>
    <p class="img-slot__hint">日中・入口付近から建物全体が入るアングル推奨／横長 16:9</p>
  </div>
</div>
```

```css
.img-slot {
  aspect-ratio: var(--ar, 16/9);
  width: 100%;
  border-radius: 12px;
  background: linear-gradient(135deg, var(--color-navy-100), #fff);
  border: 1.5px dashed var(--color-gold-500);
  display: flex; align-items: center; justify-content: center;
}
.img-slot__frame { text-align: center; color: var(--color-navy-500); padding: 24px; }
.img-slot__label { font-weight: 700; margin-top: 8px; }
.img-slot__hint { font-size: 12px; color: var(--color-text-sub); margin-top: 4px; }
```

- `data-img-hint` 属性にも同内容を入れておき、将来的にJSで画像リストを自動生成できるようにする（`document.querySelectorAll('.img-slot')` で一覧抽出可能）。
- 本番で画像を入れる際は、`.img-slot` 内の `.img-slot__frame` を丸ごと `<img src="..." alt="...">` に置き換えるだけで済む設計とする（`aspect-ratio`はコンテナ側で維持されるのでレイアウト崩れなし）。

### 3.2 サイト内で必要な画像プレースホルダー一覧（アスペクト比と推奨内容）

| 配置場所 | 比率 | 推奨画像内容 |
|---|---|---|
| home ヒーロー背景 | 21:9（大型） | 事務所の応接室 or 代表の相談風景（明るく穏やかな雰囲気） |
| home 事務所紹介ミニ | 4:3 | 代表の顔写真（バストアップ、自然光） |
| home お客様の声カード×4 | 1:1 | 依頼者風のイメージ写真（似顔絵風でも可、匿名性配慮） |
| about.html 外観 | 16:9 | 事務所の外観・入口 |
| about.html 代表写真 | 3:4 | 代表 山田誠一 のポートレート |
| about.html スタッフ写真×3 | 1:1 | 各スタッフのバストアップ |
| services-*.html 各詳細ヒーロー | 21:9 | 業務内容に応じた資料・書類・手続きイメージ |
| flow.html 各ステップアイコン画像（任意） | 1:1 | 相談風景・書類作成風景など（省略しSVGアイコンのみでも可） |
| voice.html 事例カード×5 | 1:1 | 依頼者イメージ写真 |
| column.html 記事サムネ×4 | 16:9 | 各コラム内容に応じた写真 |
| access.html 地図下 | 16:9 | 事務所最寄り駅からの道のり写真（任意、Googleマップ埋め込みで代替可） |

---

## 4. アニメーション仕様（GSAP + ScrollTrigger、CDN）

```html
<script src="https://cdnjs.cloudflare.com/ajax/libs/gsap/3.12.5/gsap.min.js"></script>
<script src="https://cdnjs.cloudflare.com/ajax/libs/gsap/3.12.5/ScrollTrigger.min.js"></script>
```

### 4.1 スクロール出現（フェード&ライズ）
- 対象：`.reveal` クラスを付与した各セクション見出し・カード・テキストブロック。
- 初期状態：`opacity:0; transform: translateY(32px);`
- ScrollTrigger `start: "top 85%"` でトリガーし、`duration:0.8, ease:"power3.out"` でフェードイン+上昇。
- カード群（業務3カテゴリ、お客様の声、スタッフ紹介等）は `stagger:0.12` で連続出現。

### 4.2 ホバー浮上（カーソルオン）
- 対象：業務カード、コラムカード、事例カード、ボタン。
- `mouseenter`で `translateY(-6px)` + `box-shadow`拡大（0.25s, ease `power2.out`）。`mouseleave`で復帰。
- タッチデバイスでは`mouseenter`が発火しないため、`:active`時に軽いスケール（0.97）演出に切替（CSSのみで十分）。

### 4.3 ヘッダーのスクロール連動変化
- §2.3参照。`ScrollTrigger`の`onUpdate`（またはシンプルに`window.scrollY`監視+`requestAnimationFrame`）でクラス切替し、GSAPで高さ・背景色・文字色を補間。

### 4.4 数値カウントアップ
- 対象：home「選ばれる理由」直後の実績数値（例：相続相談実績 ●●件、対応年数 ●年、初回相談満足度 ●%）。
- `IntersectionObserver`または`ScrollTrigger`でその要素が画面に入った瞬間に`gsap.to({val:0}, {val: 目標値, duration:1.6, ease:"power1.out", onUpdate:...})`で0→目標値までカウント。
- 和数字表現に合わせ、カウント中も3桁カンマ区切り表示。

### 4.5 ページ内の追加演出（任意で品質を底上げ）
- ヒーローのキャッチコピーは1文字ずつ/1行ずつのタイムラインでフェードイン（ページロード時、`ScrollTrigger`不要・即時実行）。
- セクション区切りの装飾線（SVG）はスクロールで左から右に伸びるドローイン演出（`stroke-dashoffset`アニメーション）。
- `prefers-reduced-motion: reduce` を尊重し、該当ユーザーには全アニメーションを即時表示（duration:0）にフォールバックする実装を必ず入れる（アクセシビリティ）。

### 4.6 GSAP CDN読み込み失敗時のフェイルセーフ（実装必須）

`.reveal`要素の初期状態を素のCSSで`opacity:0; transform:translateY(32px);`と定義してしまうと、CDN障害や広告ブロッカー等でGSAPが読み込めなかった場合に該当要素が**永久に非表示のまま**になる致命的リスクがある。これを避けるため以下の設計とする。

- `.reveal`の非表示スタイルは素のCSSには書かず、`animations.js`内でGSAPの読み込み・初期化に成功した後にのみ `document.documentElement.classList.add('js-anim-ready')` を実行し、CSS側は `.js-anim-ready .reveal { opacity:0; transform:translateY(32px); }` のようにスコープする。
- こうすることで、GSAPが読み込めなかった場合は`js-anim-ready`が付与されず、`.reveal`要素は最初から通常表示（アニメーションなしで見える状態）になり、コンテンツが消える事故を防げる。
- `<script>`タグには`onerror`ハンドラを設定し、CDN失敗を検知してもエラーで処理が止まらないようにする。

### 4.7 ScrollTrigger 初期化タイミング（実装必須）

- Noto Serif/Sans JPの読み込みでレイアウト高さが変わりうるため、`document.fonts.ready.then(() => ScrollTrigger.refresh())` をページ末尾で実行し、フォント確定後に各要素の出現位置を再計算する。
- 画像プレースホルダーは`aspect-ratio`で高さが確定しているためレイアウトシフトは発生しないが、念のためDOM構築完了後（`DOMContentLoaded`）に初期化する。
- `.reveal`や`.img-slot`等、ページによって存在しないセレクタを`gsap.utils.toArray()`で取得する箇所は、空配列でも後続処理がエラーにならないようガードする（例：`if (!els.length) return;`）。
- 数値カウントアップ（§4.4）はページごとに対象要素数が異なるため、`data-count-to`属性から目標値を読み取る汎用関数にし、要素の存在チェックを行ってから実行する。

---

## 5. ファイル構成

```
/
├── index.html
├── about.html
├── services.html
├── services-souzoku.html
├── services-fudousan.html
├── services-houjin.html
├── pricing.html
├── flow.html
├── voice.html
├── faq.html
├── column.html
├── access.html
├── contact.html
├── 404.html                          … シンプルな404ページ（ロゴ＋「ページが見つかりません」＋home/主要ページへのリンク）
├── assets/
│   ├── css/
│   │   ├── variables.css      … 配色・フォント・共通トークン
│   │   ├── base.css           … リセット・基本タイポグラフィ
│   │   ├── components.css     … ボタン・カード・img-slot・ヘッダー/フッター等
│   │   └── animations.css     … reveal初期状態、hover用トランジション
│   ├── js/
│   │   ├── nav.js             … ハンバーガーメニュー開閉、スクロールヘッダー
│   │   ├── animations.js      … GSAP ScrollTrigger初期化、カウントアップ
│   │   └── form.js            … contact.htmlのバリデーション＋ダミー送信
│   └── icons/                 … インラインSVG or 個別.svgファイル（ロゴ、UIアイコン）
└── 構成案.md（本ファイル）
```

各HTMLは `<head>`内で `variables.css → base.css → components.css → animations.css` の順に読み込み、`</body>`直前で `nav.js → animations.js`（contact.htmlのみ+`form.js`）を読み込む。ヘッダー・フッターHTMLは各ページに直接記述（§共通部分の管理方針：完全静的・依存なし）。

### 5.1 共通部分（ヘッダー/フッター/ナビ）の一貫性運用ルール

13ページに直接記述する方針上、手動修正時の変更漏れが最大のリスクとなる。以下を運用ルールとして遵守する。

- ヘッダー・フッターのHTML断片は、実装時にまず1ページ分（`index.html`）で確定させたのち、残り12ページへ**そのままコピー**して展開する（個別ページでの独自アレンジをしない）。
- 各ページのナビ内、現在ページに対応する`<a>`タグにのみ `class="is-active" aria-current="page"` を付与する差分だけを許容する（§2.4のアクティブ表示ロジック）。
- ヘッダー/フッターの構造・文言を変更する際は、`grep`等で13ファイル全てに同一の変更を機械的に反映し、最後に1ページずつ目視確認する。
- 将来的にページ数が増える、または頻繁な共通部分の更新が見込まれる場合は、fetchによるコンポーネント化や簡易ビルドスクリプトの導入を再検討する旨をコメントとして`README`等に残す（今回のデモ規模では不要と判断）。

---

## 6. home（LP）ページ 詳細構成 — `index.html`

home単体で「事務所の全体像・信頼性・相談への心理的ハードルの低さ」が完結するボリュームにする。各セクション末尾に「詳しくはこちら」で該当詳細ページに誘導する。

### 6-1. ヘッダー（§2.3の仕様。ヒーローが紺背景のため初期は透明+白文字）

### 6-2. ヒーローセクション
- 背景：`--color-navy-900`〜`--color-navy-700`グラデーション＋薄い和柄SVGパターン＋画像プレースホルダー（21:9、事務所紹介参照）を背景に重ねられる構造（画像が入ったら`background-image`的に重ねる想定、現状はグラデーションのみ表示）。
- キャッチコピー（h1、Noto Serif JP）：
  「その一歩に、まごころを。」
- サブコピー：
  「相続・不動産・会社設立のお手続きを、地域の皆さまに寄り添いながら丁寧にサポートします。初めての方も、まずはお気軽にご相談ください。」
- CTAボタン2つ横並び（モバイルは縦積み）：
  - 主：「無料相談を予約する」（ゴールド背景ボタン、`contact.html`へ）
  - 副：「今すぐ電話で相談 0120-XXX-XXX」（アウトラインボタン、`tel:`リンク）
- ヒーロー下部に「相続登記／不動産登記／会社設立 全国対応・オンライン相談可」のバッジ帯。

### 6-3. 選ばれる理由（4点）
見出し：「まごころ司法書士事務所が選ばれる理由」
4カードを`.reveal`+`stagger`でグリッド表示（PC4列/タブレット2列/モバイル1列）：
1. 「初回相談は無料」— 費用の不安なく、まずはじっくりお話しいただけます。
2. 「土日・オンライン相談対応」— お仕事や介護でお忙しい方にも寄り添います。
3. 「明朗な料金体系」— お見積り後の追加費用は原則なし。安心の価格表示。
4. 「地域密着●年」— みのり市エリアで長年蓄積したノウハウで対応します。

### 6-4. 実績数値カウントアップ
帯セクション（`--color-navy-900`背景・白文字）に3つの数値を横並び：
- 「累計ご相談件数」 **1,200** 件〜
- 「対応年数」 **18** 年
- 「初回相談後のご継続率」 **96** ％

### 6-5. 業務案内（3カテゴリカード）
見出し：「対応業務」サブ見出し：「相続・不動産・会社設立、幅広くワンストップでサポートします」
3カード（画像プレースホルダー16:9＋アイコン＋タイトル＋2行説明＋「詳しくはこちら▶」）：
1. 相続・遺言サポート → `services-souzoku.html`
   「相続登記の義務化にも対応。遺言書作成から遺産分割、相続登記まで一括サポート。」
2. 不動産登記 → `services-fudousan.html`
   「売買・贈与・住宅ローン完済時の抹消登記まで、不動産に関する登記を幅広く対応。」
3. 法人登記 → `services-houjin.html`
   「会社設立・役員変更・本店移転など、法人のライフサイクルに合わせてサポート。」
セクション末尾中央に「業務案内をすべて見る▶」（`services.html`へ）。

### 6-6. 事務所・代表紹介（簡易版）
2カラム（画像プレースホルダー4:3＋テキスト）：
- 見出し：「代表からのごあいさつ」
- 本文（150字程度）：「はじめまして、代表の山田誠一です。人生の大きな節目に関わる手続きだからこそ、専門知識と同じくらい"寄り添う気持ち"を大切にしています。どうぞお気軽にお声がけください。」
- 「事務所紹介を詳しく見る▶」（`about.html`へ）

### 6-7. ご相談の流れ（4ステップ）
横並びステップ（モバイル縦）、番号+アイコン+短文：
1. お問い合わせ（電話・フォーム・LINEは今回なし）
2. 無料相談（対面・オンライン選択可）
3. お見積り・ご契約
4. 手続き開始〜完了のご報告
「流れを詳しく見る▶」（`flow.html`へ）

### 6-8. 料金の目安
3プランカードの抜粋（詳細は`pricing.html`）：
- 相続登記サポート：66,000円〜（登録免許税等の実費別途）
- 不動産名義変更（売買・贈与）：55,000円〜
- 株式会社設立サポート：88,000円〜（定款認証・登録免許税別途）
「料金を詳しく見る▶」（`pricing.html`へ）

### 6-9. お客様の声（事例抜粋・カード4件）
画像プレースホルダー1:1＋★評価＋コメント抜粋＋属性（60代・女性・相続登記など）。4件を`.reveal`+`stagger`表示。
「お客様の声をもっと見る▶」（`voice.html`へ）

### 6-10. 対応エリア・FAQ抜粋
- 対応エリア：「みのり県はなさき市を中心に、近隣市町村まで対応。オンライン相談は全国対応可能です。」（簡易地図プレースホルダーは省略、テキストのみで可）
- FAQ抜粋3問（アコーディオンで開閉、`.reveal`）：
  1. Q. 相談だけでも大丈夫ですか？ / A. もちろんです。ご契約前提ではございません。
  2. Q. 平日は仕事で行けないのですが？ / A. 土曜相談・オンライン相談に対応しております。
  3. Q. 費用はいつ分かりますか？ / A. 無料相談後、お見積りを書面でご提示します。
「FAQをもっと見る▶」（`faq.html`へ）

### 6-11. 最終CTA
帯セクション（ゴールド系背景 or 紺背景+ゴールド枠）：
見出し：「まずは、無料相談から。」
本文：「一人で悩まず、まずはお話をお聞かせください。」
ボタン：「無料相談を予約する」（`contact.html`）＋電話番号

### 6-12. フッター（§2.5）

---

## 7. 詳細ページ 詳細構成

各ページ共通ヘッダー：初期状態から白背景（`data-header-theme="light"`）、ヒーロー帯（`--color-navy-900`背景、画像プレースホルダー21:9を重ねられる構造）にページタイトルの英字/和字見出しを表示。

### 7-1. `about.html` 事務所紹介
- ヒーロー：「事務所紹介」
- 事務所概要テーブル：事務所名／代表者名／所在地（架空）／電話番号（架空）／設立年（架空）／対応時間（平日9:00-18:00、土曜相談可・要予約）
- 外観画像プレースホルダー（16:9）
- 代表あいさつ（フル版、400字程度）＋代表写真プレースホルダー（3:4）
- スタッフ紹介カード×3（写真1:1＋氏名＋役職＋一言）：
  1. 佐藤 陽子（司法書士）
  2. 高橋 美咲（パラリーガル）
  3. 中村 健太（パラリーガル）
- 事務所の理念セクション（3つの約束：「誠実な対応」「明朗な料金」「迅速な対応」）
- 末尾CTA（無料相談予約）

### 7-2. `services.html` 業務案内トップ
- ヒーロー：「業務案内」
- 導入文＋3カテゴリカード（home同様だが説明文はより詳細・各150字程度）
- 各カードから該当詳細ページへ
- ワンストップ対応の強みセクション（提携先：税理士・弁護士等との連携、架空で可）
- 末尾CTA

### 7-3. `services-souzoku.html` 相続・遺言サポート
- ヒーロー画像プレースホルダー（21:9）
- 対応内容リスト：相続登記／遺産分割協議書作成／遺言書作成サポート（自筆証書・公正証書）／成年後見制度利用支援／相続放棄サポート
- 「こんな方におすすめ」チェックリスト4〜5項目
- 手続きの流れ（このページ内で相続特化の5ステップ）
- 料金目安（相続関連プラン抜粋、`pricing.html`への導線）
- よくある質問（相続特化3問）
- 末尾CTA

### 7-4. `services-fudousan.html` 不動産登記
- 対応内容：売買による所有権移転登記／贈与登記／住宅ローン完済による抵当権抹消登記／住所変更登記
- 「こんな方におすすめ」／流れ／料金目安／FAQ／末尾CTA（構成は7-3と同型）

### 7-5. `services-houjin.html` 法人登記
- 対応内容：株式会社・合同会社設立登記／役員変更登記／本店移転登記／商号・目的変更登記
- 「こんな方におすすめ」／流れ／料金目安／FAQ／末尾CTA（構成は7-3と同型）
- **トーン差別化（他2ページとの違い）**：相続・不動産ページは「寄り添い・安心」を前面に出す文言だが、本ページのみターゲットが起業家・若手経営者中心になるため、**スピード感・効率性を訴求するトーン**に切り替える。
  - キャッチコピー例：「会社設立、最短即日オンライン相談。スピード対応で事業スタートを止めない。」
  - CTA文言も「無料相談を予約する」に加え「オンラインで今すぐ相談」を優先表示。
  - 「こんな方におすすめ」も「登記のためだけに平日仕事を休みたくない方」「複数拠点の会社を運営していてスピーディに対応してほしい方」等、経営者目線の文言にする。

### 7-6. `pricing.html` 料金案内
- 導入文：「追加費用が発生しないよう、事前見積りを徹底しています」
- カテゴリ別（相続／不動産／法人）に料金テーブル（業務内容・報酬額目安・実費目安の3列）
- 「report登録免許税・実費について」の補足説明ボックス
- お支払い方法（現金・銀行振込・カード、架空で可）
- 末尾CTA

### 7-7. `flow.html` ご相談の流れ
- home の4ステップをより詳細な5〜6ステップに拡張（各ステップに所要目安時間・準備物を明記）
- 「オンライン相談の流れ」補足セクション
- 末尾CTA

### 7-8. `voice.html` お客様の声・解決事例
- 事例カード5〜6件（画像プレースホルダー1:1、属性、課題→解決の簡単なストーリー3〜4行、★評価）
- カテゴリフィルタ（相続／不動産／法人）をタブ切り替え（JSでフィルタ、アニメーション付き）
- 末尾CTA

### 7-9. `faq.html` よくあるご質問
- カテゴリ別（相談全般／相続／不動産／法人／料金）にアコーディオンFAQ、各カテゴリ3〜4問、計15問前後
- 末尾CTA

### 7-10. `column.html` コラム一覧
- サンプル記事4本のカード一覧（画像プレースホルダー16:9＋タイトル＋日付＋カテゴリタグ＋抜粋）
  1. 「相続登記の義務化、何が変わった？」
  2. 「不動産の名義変更、自分でもできる？専門家に頼むメリット」
  3. 「会社設立、司法書士と行政書士どちらに相談すべき？」
  4. 「遺言書は書いた方がいい？3つのケースで解説」
- ※記事詳細ページは本デモの範囲外とし、カードはリンクなし（`href="#"`+将来実装コメント）とする
- 末尾CTA

### 7-11. `access.html` アクセス
- 所在地・最寄り駅からの道のり（架空）
- Googleマップ埋め込み用の`iframe`枠（今回はプレースホルダー：地図画像プレースホルダー16:9で代替、コメントで実装時のiframe差し替え案内）
- 駐車場情報、営業時間、休業日
- 末尾CTA

### 7-12. `contact.html` お問い合わせ（無料相談予約）

初回接触のハードルを下げるため、**必須項目は最小限（3項目）に絞る**。フォーム冒頭に「入力は1分程度で完了します」の一言を添え、心理的ハードルを下げる。

- 必須：お名前＊／電話番号＊／ご相談内容（相続／不動産／法人／その他のいずれかを選択＊）
- 任意：フリガナ／メールアドレス／ご相談方法（対面・オンライン・電話のラジオ、未選択なら「担当より確認」）／ご希望日時／お問い合わせ内容（テキストエリア）
- 個人情報の取り扱いについての同意チェックボックス＊（必須）
- 任意項目には`(任意)`ラベルを明示し、必須項目には視覚的に分かるアスタリスク＋`aria-required="true"`を付与。

**フォームアクセシビリティ実装仕様（必須）**
- 全入力要素は`<label for="...">`と`id`で明示的に関連付ける（プレースホルダーのみに頼らない）。
- バリデーションエラーは各入力欄の直下に赤文字＋アイコンで表示し、エラー文言を含むコンテナに`aria-live="polite"`を設定して支援技術に通知する。
- 送信ボタン押下時にエラーがあれば、画面最上部のエラーサマリー（`role="alert"`）にフォーカスを移動し、「未入力の項目が3件あります」等の要約と各エラー項目へのアンカーリンクを表示する。
- クライアント側バリデーション（必須項目・電話番号形式）をJSで実装。メールアドレスは任意項目のため、入力があった場合のみ形式チェックする。
- 送信ボタン押下→ダミー送信（実際の送信処理はなし）→「送信しました」の完了メッセージ表示（同ページ内でフォームを完了メッセージに差し替え、GSAPでフェード切替）。完了メッセージにも`role="status"`を付与し、フォーカスを完了メッセージの見出しへ移動する。
- サイドに電話番号・営業時間・LINEなしの補足

---

## 8. レスポンシブ・アクセシビリティ方針

- ブレークポイント：`~375px`（最小モバイル）／`768px`（タブレット）／`1024px`（デスクトップ）／`1440px`（大型デスクトップ、コンテンツ最大幅1200pxで中央寄せ）
- モバイルファーストでCSS記述（`min-width`メディアクエリ）
- コントラスト比：本文は`--color-text-main`(#2A2A2A) on `--color-bg`で AA基準クリア。ボタンのゴールド×紺文字もコントラスト確認済みの組み合わせを使用。
- フォーカスインジケーターをボタン・リンクに明示（`:focus-visible`でアウトライン表示、デザインを損なわない色で）
- `prefers-reduced-motion`対応（§4.5）
- 全画像プレースホルダーに`alt`相当のテキスト説明を`data-img-hint`および将来の`alt`属性下書きとして保持

### 8.1 タイポグラフィ・タップ領域の数値基準（高齢層ユーザーへの配慮として必須）

相続・遺言サポート等は高齢層の閲覧が多いことを踏まえ、以下を最低基準として明記する。

| 項目 | 基準値 |
|---|---|
| 本文フォントサイズ（モバイル） | 16px以上（`--fs-body: 16px`） |
| 本文フォントサイズ（デスクトップ） | 17〜18px（`--fs-body-lg: 18px`） |
| 本文行間（line-height） | 1.8前後 |
| 見出しh1 | モバイル28px／デスクトップ40〜48px |
| 見出しh2 | モバイル22px／デスクトップ32px |
| 見出しh3 | モバイル18px／デスクトップ22px |
| リンク・ボタンのタップ領域 | 最小44×44px（WCAG 2.5.5準拠）、ナビ項目やFAQアコーディオンの見出し行も同基準を満たす縦paddingを確保 |
| 本文の最大文字幅 | 1行あたり全角35〜40文字程度で折り返し（`max-width`で制御し可読性を担保） |

これらは`variables.css`にCSS変数として定義し、全ページで参照する。

## 9. SEO / メタ情報方針

- 各ページに個別の`<title>`（例：「相続・遺言サポート｜まごころ司法書士事務所（デモサイト）」）と`<meta name="description">`
- OGPタグは記述するが`og:image`は未設定（画像確定後に追加とコメントを残す）
- 構造化データ（LocalBusiness等）は本デモでは省略（架空情報のため実装しない）
- 全ページの`<html>`タグに`lang="ja"`を明記
- 各ページに自ページを指す`<link rel="canonical" href="...">`を設定（重複コンテンツ評価を避ける）
- 存在しないURLアクセス時のために`404.html`を用意し、ロゴ・簡潔な案内文・home及び主要ページへのリンクを設置（§5のファイル構成に追加済み）

---

以上を構成案の確定版（v1.1）とする。UI/UXデザイナー役・エンジニア役のサブエージェントによるレビューを反映済みであり、本ドキュメントをそのまま実装フェーズのプロンプト/仕様書として使用する。
